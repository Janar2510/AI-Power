# ERP Platform dependencies
Jinja2>=3.0
Werkzeug>=2.0
psycopg2-binary>=2.9
passlib[bcrypt]>=1.7
simple-websocket>=1.0
# Optional for --gevent-websocket (production WebSocket): pip install gevent
# Optional for --workers=N (multi-worker prefork): pip install gunicorn
# Optional for Phase 136 vector RAG: pip install pgvector
# Phase 151 PDF reports: pip install weasyprint
weasyprint>=60.0
pyotp>=2.9
qrcode>=7.4
